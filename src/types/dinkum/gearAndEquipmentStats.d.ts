export interface GearAndEquipmentStatsProps {
  bookCollection: {
    [key: string]: boolean;
  };
  toolCollection: {
    [key: string]: boolean;
  };
  weaponCollection: {
    [key: string]: boolean;
  };
  equipmentCollection: {
    [key: string]: boolean;
  };
  vehicleCollection: {
    [key: string]: boolean;
  };
}
